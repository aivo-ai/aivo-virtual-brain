apiVersion: v2
name: inference-gateway-svc
description: AIVO Inference Gateway Service Helm Chart
type: application
version: 0.1.0
appVersion: "1.0.0"

keywords:
  - inference
  - ai
  - microservice
  - aivo
  - ml

maintainers:
  - name: AIVO Platform Team
    email: platform@aivo.dev

dependencies:
  - name: redis
    version: "17.0.0"
    repository: "https://charts.bitnami.com/bitnami"
    condition: redis.enabled

annotations:
  category: AI/ML
