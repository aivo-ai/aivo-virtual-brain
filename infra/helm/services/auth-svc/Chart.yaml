apiVersion: v2
name: auth-svc
description: AIVO Authentication Service Helm Chart
type: application
version: 0.1.0
appVersion: "1.0.0"

keywords:
  - authentication
  - microservice
  - aivo
  - security

maintainers:
  - name: AIVO Platform Team
    email: platform@aivo.dev

dependencies:
  - name: redis
    version: "17.0.0"
    repository: "https://charts.bitnami.com/bitnami"
    condition: redis.enabled
  - name: postgresql
    version: "11.0.0"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled

annotations:
  category: Authentication
